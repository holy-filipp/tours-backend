meta {
  name: Fetch CSRF
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/sanctum/csrf-cookie
  body: none
  auth: inherit
}

script:post-response {
  // bru.setVar("XSRF-TOKEN", decodeURIComponent(pm.cookies.get("XSRF-TOKEN")))
}

settings {
  encodeUrl: true
}
